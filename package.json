{
  "name": "ai-coding-agent-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "build": "npm --prefix frontend run build && npm --prefix backend run build && cp -r backend/src/templates backend/dist/templates",
    "start": "npm run build && node backend/dist/index.js",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build"
  }
}
